<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:camel="http://camel.apache.org/schema/spring"

       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">

    <bean class="net.bk.SampleBean" id="myBean"/>

    <!-- Camel Context -->
    <camelContext xmlns="http://camel.apache.org/schema/spring">
        <!--<camel:routeBuilder ref="myRouteBuilder"/>-->

        <!--<route id="timer-to-console">-->
            <!--<from uri="timer://foo?fixedRate=true&amp;period={{timer.period}}"/>-->
            <!--<transform>-->
                <!--<method bean="myBean" method="saySomething">-->
                <!--</method>-->
            <!--</transform>-->
            <!--<to uri="stream:out"/>-->
        <!--</route>-->


        <!--<route id="test">-->
            <!--<from uri="activemq:queue:inbox"/>-->
            <!--<log message="Routing message from inbox to outbox queue with data ${body}"/>-->
            <!--<to uri="activemq:queue:outbox"/>-->
        <!--</route>-->

        <route id="test1">
            <from uri="activemq-data:queue:inbox"/>
            <log message="Routing message from inbox to outbox queue with data ${body}"/>
            <to uri="activemq-data:queue:outbox"/>
            <to uri="file://temp/activemq/?fileExist=Append&amp;fileName=queue-${date:now:yyyyMMdd}.txt" />
        </route>





    </camelContext>


    <bean id="activemq-data" class="org.apache.activemq.camel.component.ActiveMQComponent">
        <property name="brokerURL" value="${amq.brokerurl}" />
        <property name="userName" value="${amq.user}" />
        <property name="password" value="${amq.password}" />
        <property name="concurrentConsumers" value="2" />
    </bean>
</beans>